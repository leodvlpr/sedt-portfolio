---
import content from "../data/content.json";
import SectionHeading from "./SectionHeading.astro";

type Job = {
  company: string;
  role: string;
  current?: boolean;
  domain?: string;
};

const experience: Job[] = content.experience;

/* Nota: Softvision apunta a cognizant.com (empresa adquirente) — intencional. */

/* logo.clearbit.com dejó de resolver (Clearbit fue absorbida por HubSpot y la Logo
   API se discontinuó). Usamos el servicio de iconos de DuckDuckGo, que devuelve
   marcas reales de 32-48px para estos dominios. Si algún día también cae, el
   onerror deja a la vista el monograma que hay debajo. */
const logoUrl = (domain: string) =>
  `https://icons.duckduckgo.com/ip3/${domain}.ico`;

const rowClass =
  "flex items-center gap-4 rounded-sm border border-ink/15 px-4 py-4";
---

<section id="experience" aria-labelledby="experience-title" class="py-14">
  <SectionHeading title="Experience" id="experience" />

  <ul class="mt-6 flex flex-col gap-3">
    {
      experience.map((job) => {
        const hasDomain = Boolean(job.domain);

        const body = (
          <>
            <span
              aria-hidden="true"
              class="relative grid h-8 w-8 shrink-0 place-items-center overflow-hidden rounded-xs border border-ink/15"
            >
              <span class="text-xs font-medium text-ink/50">
                {job.company.charAt(0)}
              </span>
              {hasDomain && (
                <img
                  src={logoUrl(job.domain!)}
                  alt=""
                  width="32"
                  height="32"
                  loading="lazy"
                  decoding="async"
                  onerror="this.remove()"
                  class="absolute inset-0 h-full w-full bg-paper object-contain"
                />
              )}
            </span>

            <span class="min-w-0 flex-1">
              <span class="flex flex-wrap items-center gap-x-2 gap-y-1">
                <span class="font-medium">{job.company}</span>
                {job.current && (
                  <span class="rounded-xs border border-coral px-1.5 py-0.5 text-[10px] font-medium uppercase tracking-wider text-coral">
                    Actual
                  </span>
                )}
              </span>
              <span class="mt-0.5 block text-sm text-ink/60">{job.role}</span>
            </span>
          </>
        );

        return (
          <li>
            {hasDomain ? (
              <a
                href={`https://${job.domain}`}
                target="_blank"
                rel="noopener noreferrer"
                class={`${rowClass} hover:border-coral`}
              >
                {body}
              </a>
            ) : (
              <div class={rowClass}>{body}</div>
            )}
          </li>
        );
      })
    }
  </ul>
</section>
